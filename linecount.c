#include <stdio.h>
int main()
{
    printf("I am alive! beware. \n");
    getchar();
    /*getchar - reads a single character and waits for user to hit enter before  reading the
     * character.... to prevent window from closing before you see output */

    return 0;
}

